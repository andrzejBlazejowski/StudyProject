<UserControl x:Class="StudyProject.Components.navComponent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StudyProject.Components"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="800">
    <Grid>
        <ToolBarTray Width="150" Orientation="Vertical" HorizontalAlignment="Left">
            <ToolBar  Width="150">
                <Button Command="{Binding NavigateAllBrandsCmd}" ToolTip="lista Pracownikow.">
                    <Label>Producenci / Marki</Label>
                    <!-- <Image Source="" />-->
                </Button>

                <Button Command="{Binding NavigateAllComoditiesCmd}" ToolTip="lista Pracownikow.">
                    <Label>Produkty / Towary</Label>
                </Button>

                <Button Command="{Binding NavigateAllComodityCategoryCmd}" ToolTip="lista Pracownikow.">
                    <Label>typy produktow</Label>
                </Button>


                <Button Command="{Binding NavigateAllContractorsCmd}" ToolTip="lista Pracownikow.">
                    <Label>Kontrachenci</Label>
                </Button>

                <Button Command="{Binding NavigateAllContractorTypesCmd}" ToolTip="lista Pracownikow.">
                    <Label>typy kontrachentow</Label>
                </Button>

                <Button Command="{Binding NavigateAllCurenciesCmd}" ToolTip="lista Pracownikow.">
                    <Label>Waluty</Label>
                </Button>

                <Button Command="{Binding NavigateAllDeliveryItemsCmd}" ToolTip="lista Pracownikow.">
                    <Label>pozycje dostawy</Label>
                </Button>

                <Button Command="{Binding NavigateAllDeliveryStatusesCmd}" ToolTip="lista Pracownikow.">
                    <Label>statusy dostawy</Label>
                </Button>

                <Button Command="{Binding NavigateAllDeliveryCmd}" ToolTip="lista Pracownikow.">
                    <Label>Dostawy</Label>
                </Button>

                <Button Command="{Binding NavigateAllEmplyeesCmd}" ToolTip="lista Pracownikow.">
                    <Label>Pracownicy</Label>
                </Button>

                <Button Command="{Binding NavigateAllEmplyeeTypesCmd}" ToolTip="lista Pracownikow.">
                    <Label>typy pracownikow</Label>
                </Button>

                <Button Command="{Binding NavigateAllInvoicesCmd}" ToolTip="lista Pracownikow.">
                    <Label>Faktury</Label>
                </Button>

                <Button Command="{Binding NavigateAllInvoiceItemsCmd}" ToolTip="lista Pracownikow.">
                    <Label>pozycje faktury</Label>
                </Button>

                <Button Command="{Binding NavigateAllPaymentMethodsCmd}" ToolTip="lista Pracownikow.">
                    <Label>metody platnosci</Label>
                </Button>

                <Button Command="{Binding NavigateAllSizeTypesCmd}" ToolTip="lista Pracownikow.">
                    <Label>rozmiary</Label>
                </Button>

                <Button Command="{Binding NavigateAllStoragesCmd}" ToolTip="lista Pracownikow.">
                    <Label>miejsca</Label>
                </Button>

                <Button Command="{Binding  NavigateAllWarehousesCmd}" ToolTip="lista Pracownikow.">
                    <Label>magazyny</Label>
                </Button>


                <Button Command="{Binding NavigateAddBrandsCmd}" ToolTip="lista Pracownikow.">
                    <Label>dodaj Producenci / Marki</Label>
                    <!-- <Image Source="" />-->
                </Button>

                <Button Command="{Binding NavigateAddComoditiesCmd}" ToolTip="lista Pracownikow.">
                    <Label>dodaj Produkty / Towary</Label>
                </Button>

                <Button Command="{Binding NavigateAddComodityCategoryCmd}" ToolTip="lista Pracownikow.">
                    <Label>dodaj typy produktow</Label>
                </Button>


                <Button Command="{Binding NavigateAddContractorsCmd}" ToolTip="lista Pracownikow.">
                    <Label>dodaj Kontrachenci</Label>
                </Button>

                <Button Command="{Binding NavigateAddContractorTypesCmd}" ToolTip="lista Pracownikow.">
                    <Label>dodaj typy kontrachentow</Label>
                </Button>

                <Button Command="{Binding NavigateAddCurenciesCmd}" ToolTip="lista Pracownikow.">
                    <Label>dodaj Waluty</Label>
                </Button>

                <Button Command="{Binding NavigateAddDeliveryItemsCmd}" ToolTip="lista Pracownikow.">
                    <Label>dodaj pozycje dostawy</Label>
                </Button>

                <Button Command="{Binding NavigateAddDeliveryStatusesCmd}" ToolTip="lista Pracownikow.">
                    <Label>dodaj statusy dostawy</Label>
                </Button>

                <Button Command="{Binding NavigateAddDeliveryCmd}" ToolTip="lista Pracownikow.">
                    <Label>dodaj Dostawy</Label>
                </Button>

                <Button Command="{Binding NavigateAddEmplyeesCmd}" ToolTip="lista Pracownikow.">
                    <Label>dodaj Pracownicy</Label>
                </Button>

                <Button Command="{Binding NavigateAddEmplyeeTypesCmd}" ToolTip="lista Pracownikow.">
                    <Label>dodaj typy pracownikow</Label>
                </Button>

                <Button Command="{Binding NavigateAddInvoicesCmd}" ToolTip="lista Pracownikow.">
                    <Label>dodaj Faktury</Label>
                </Button>

                <Button Command="{Binding NavigateAddInvoiceItemsCmd}" ToolTip="lista Pracownikow.">
                    <Label>dodaj pozycje faktury</Label>
                </Button>

                <Button Command="{Binding NavigateAddPaymentMethodsCmd}" ToolTip="lista Pracownikow.">
                    <Label>dodaj metody platnosci</Label>
                </Button>

                <Button Command="{Binding NavigateAddSizeTypesCmd}" ToolTip="lista Pracownikow.">
                    <Label>dodaj rozmiary</Label>
                </Button>

                <Button Command="{Binding NavigateAddStoragesCmd}" ToolTip="lista Pracownikow.">
                    <Label>dodaj miejsca</Label>
                </Button>

                <Button Command="{Binding  NavigateAddWarehousesCmd}" ToolTip="lista Pracownikow.">
                    <Label>dodaj magazyny</Label>
                </Button>

            </ToolBar>
        </ToolBarTray>
    </Grid>
</UserControl>
